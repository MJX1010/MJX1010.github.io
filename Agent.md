# Agent 工作规范

本文件定义本仓库中 Agent 进行笔记整理、链接归档和知识库维护时必须遵守的规则。

## 目标

- 以“知识沉淀”为目标，而不是“链接收藏”。
- 任何一批导入资料都必须完成全量整理，不允许长期保留“待整理笔记”。
- 笔记应与现有主题体系合并，必要时新增二级、三级、四级分类，但首层分类保持简洁、概括性强。

## 强制规则

1. **全量清空待整理**
   - 当发现 `_temp`、OneTab、收集箱、导入批次等来源时，必须对该批次做完整处理。
   - 处理结果只能是：并入正式笔记、归入私有归档、明确淘汰。
   - 不允许留下“待继续分类”“以后再整理”“临时收纳”作为长期状态。
   - 不允许保留带“待整理”语义的最终文件名、标题或目录节点；收尾时必须改写为“已归档记录”或直接删除。

2. **禁止只贴链接**
   - 不得把外链直接原样粘贴进公开笔记正文作为主要内容。
   - 必须先访问原链接内容，提取其核心信息、适用场景、限制条件、与现有笔记的关系，再合并整理。
   - 只有在原链接仍具有回看价值时，才将其保留到 `## 参考链接`。

3. **优先合并现有笔记**
   - 先查找是否已有对应专题笔记。
   - 若已有笔记，优先把新信息合并进正文，而不是新建平行或重复笔记。
   - 仅当现有笔记无法承载、且主题已稳定独立时，才新建专题。

4. **公开与私有分流**
   - 需要登录态、公司账号、内网、本地地址、控制台、Jenkins、交易页、个人项目页等，进入根目录 `private/`。
   - 公开区 `content/` 只保留可长期复用、可公开访问、对知识库读者有持续价值的内容。

5. **清理重复与低价值入口**
   - 同一工具只保留一个主入口，优先官方主页、官方文档或主仓库。
   - `releases`、`issues`、搜索结果页、营销落地页、镜像页一般不作为主入口保留。
   - 单条 issue / 单篇帖子只有在正文已吸收其关键结论时，才可作为补充引用。

## 整理流程

1. 读取导入来源，列出全部条目。
2. 判断每个条目属于公开、私有、淘汰中的哪一类。
3. 对准备进入公开区的条目，访问原文并提取关键信息。
4. 将信息并入现有专题正文，补齐适用场景、边界和风险。
5. 只为仍有回看价值的原始来源保留参考链接。
6. 检查重复链接、无效链接、重复标题和结构冲突。
7. 将导入批次改写为“已归档记录”或删除，不保留“待整理”状态。

## 笔记结构要求

- 正文优先写结论、分类、流程、适用场景、限制条件。
- `## 参考链接` 只放正文之外仍值得回看的原始资料。
- frontmatter 统一维护 `status`、`confidence`、`visibility`、`last_curated`、`source_count`。
- 对入口型笔记，要逐步把“链接清单”升级为“主题综述 + 少量主入口”。

## 变更完成标准

- 当前批次不存在“待整理公开链接”。
- 不存在明显重复笔记或重复主入口。
- 新增内容已被归入正式分类。
- 私有内容已移出公开区。
- 修改后的 Markdown 通过基础诊断检查。
